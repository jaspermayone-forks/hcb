<details class="dock" <%= "open" if local_assigns[:selected].to_s.starts_with?("settings") %>>
  <summary class="dock__item tooltipped tooltipped--e" aria-label="Edit account settings">
    <%= inline_icon "settings", size: 30, style: "padding: 0" %>
    <span class="flex-grow">
      Settings
    </span>
    <%= inline_icon "up-caret-small", class: "drawer-chevron fill-muted" %>
  </summary>
  <%= dock_item "Account",
                @user == current_user ? my_settings_path : edit_user_path(@user || current_user),
                selected: local_assigns[:selected] == :settings_home %>
  <%= dock_item "Notifications",
                @user == current_user ? settings_notifications_path : notifications_user_path(@user || current_user),
                selected: local_assigns[:selected] == :settings_notifications %>
  <%= dock_item "Billing address",
                @user == current_user ? settings_address_path : address_user_path(@user || current_user),
                selected: local_assigns[:selected] == :settings_billing_address %>
  <%= dock_item "Reimbursement payouts",
                @user == current_user ? settings_payouts_path : payouts_user_path(@user || current_user),
                selected: local_assigns[:selected] == :settings_reimbursement_payouts %>
  <%= dock_item "Security",
                @user == current_user ? settings_security_path : security_user_path(@user || current_user),
                selected: local_assigns[:selected] == :settings_security %>
  <%= dock_item "Feature previews",
                @user == current_user ? settings_previews_path : previews_user_path(@user || current_user),
                selected: local_assigns[:selected] == :settings_feature_previews %>
  <%= dock_item "Integrations",
                @user == current_user ? settings_integrations_path : integrations_user_path(@user || current_user),
                selected: local_assigns[:selected] == :settings_integrations %>
  <%= dock_item "Admin",
                @user == current_user ? settings_admin_path : admin_user_path(@user || current_user),
                selected: local_assigns[:selected] == :settings_admin, admin: true %>
</details>
